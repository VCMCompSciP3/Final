# Blackjack AI — Python dependencies
# Install with: pip install -r requirements.txt

# ── Data & ML (ml/ scripts) ──────────────────────────────────────────────────
pandas>=2.0.0          # CSV loading and feature engineering
scikit-learn>=1.4.0    # RandomForestClassifier, train_test_split, metrics
joblib>=1.3.0          # Save and load the trained model (.pkl)

# ── Web server (backend/) ────────────────────────────────────────────────────
flask>=3.0.0           # Lightweight Python web framework
flask-cors>=4.0.0      # Allows frontend (different port) to call Flask API
numpy>=1.26.0          # Feature array for model.predict() in app.py